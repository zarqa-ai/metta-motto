(= (respond)
   (superpose
       (
        (add-langchain-history  (messages))
         (py-dot  ((py-dot (get-langchain-model)  invoke) (get-langchain-history)) content)
       )
   )

)
!(Response (respond) )





;!(import! &self langchain_examples:langchain_states)
(= (respond)
   (let () (add-langchain-history  (messages))
         (let $ans (py-dot  ((py-dot (get-langchain-model)  invoke) (get-langchain-history)) content)
              (let()
                 (add-langchain-history  (assistant  $ans ))
                 $ans
               )
         )
   )
)
!(Response (respond ))





